name: 构建 Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4

    - name: ☕ 设置 JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        cache: 'gradle'

    - name: 🔧 赋予 Gradle 执行权限
      run: chmod +x android/gradlew

    - name: 📦 构建 Debug APK
      run: |
        cd android
        ./gradlew assembleDebug --stacktrace

    - name: 📝 重命名 APK
      run: |
        mkdir -p release
        cp android/app/build/outputs/apk/debug/app-debug.apk release/应用整理工具-debug.apk

    - name: 🎉 上传 APK
      uses: actions/upload-artifact@v4
      with:
        name: 应用整理工具-APK
        path: release/应用整理工具-debug.apk
        retention-days: 30

    - name: 📊 获取 APK 信息
      run: |
        echo "APK 构建成功！"
        ls -lh release/
        echo "文件大小: $(du -h release/应用整理工具-debug.apk | cut -f1)"
