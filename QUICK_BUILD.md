# 🚀 快速打包 APK 指南

## ⚡ 最简单的方法（只需2步）

### 前提条件
✅ 已安装 Android Studio（会自动配置好所有工具）

---

## 🎯 方法一：双击脚本自动打包（推荐）

### 步骤：

1. **用 Android Studio 打开项目**
   - 打开 `android` 文件夹
   - 等待 Gradle 同步完成（首次需要下载依赖）

2. **双击运行打包脚本**
   ```
   双击：build_apk.bat
   ```

3. **等待构建完成**
   - 会自动清理、编译、打包
   - 约需 2-5 分钟（首次可能更久）

4. **获取 APK**
   ```
   生成位置：release/应用整理工具.apk
   ```

5. **传到手机安装**
   - 用微信、QQ 发给自己
   - 或用数据线复制到手机
   - 点击安装即可

---

## 🎯 方法二：Android Studio 图形界面

### 步骤：

1. **打开项目**
   - Android Studio → Open → 选择 `android` 文件夹

2. **开始构建**
   ```
   菜单栏：Build → Build Bundle(s) / APK(s) → Build APK(s)
   ```

3. **等待构建**
   - 底部状态栏显示进度
   - 完成后会弹出提示

4. **点击提示中的 "locate"**
   - 自动打开 APK 所在文件夹
   - 文件名：`app-debug.apk`

5. **传到手机安装**

---

## 🎯 方法三：命令行打包

### 步骤：

1. **打开终端**
   ```bash
   cd android
   ```

2. **执行构建命令**
   ```bash
   # Windows
   gradlew.bat assembleDebug
   
   # Mac/Linux
   ./gradlew assembleDebug
   ```

3. **找到 APK**
   ```
   位置：android/app/build/outputs/apk/debug/app-debug.apk
   ```

---

## 📦 生成的 APK 说明

### 文件信息
- **文件名**：`app-debug.apk` 或 `应用整理工具.apk`
- **类型**：Debug 版本（未签名）
- **大小**：约 5-10 MB
- **用途**：个人使用、测试

### Debug vs Release

| 类型 | 用途 | 签名 | 性能 | 上架 |
|------|------|------|------|------|
| Debug | 开发测试 | 自动签名 | 稍慢 | ❌ 不能 |
| Release | 正式发布 | 需要自己签名 | 优化 | ✅ 可以 |

**现在生成的是 Debug 版本，够用了！**

---

## 🔧 常见问题

### Q1: gradlew 不是内部或外部命令？
**A**: 需要先用 Android Studio 打开项目，它会自动生成 gradlew

### Q2: 构建失败？
**A**: 检查以下几点：
1. Android Studio 是否安装完整
2. 项目是否用 Android Studio 打开过
3. Gradle 依赖是否下载完成
4. 查看错误信息，通常会提示缺少什么

### Q3: 找不到 JDK？
**A**: Android Studio 自带 JDK，确保：
- 使用 Android Studio 自带的终端
- 或设置环境变量 `JAVA_HOME`

### Q4: Gradle 下载慢？
**A**: 使用国内镜像：
在 `android/build.gradle` 顶部添加：
```gradle
allprojects {
    repositories {
        maven { url 'https://maven.aliyun.com/repository/google' }
        maven { url 'https://maven.aliyun.com/repository/jcenter' }
        maven { url 'https://maven.aliyun.com/repository/public' }
        google()
        mavenCentral()
    }
}
```

### Q5: 手机无法安装？
**A**: 
1. 开启"允许安装未知来源应用"
2. 关闭"纯净模式"（小米）
3. 如果之前装过，先卸载再装

---

## 🎁 生成 Release 版本（可选）

如果想生成正式版本：

### 1. 创建签名密钥
```bash
keytool -genkey -v -keystore my-release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias my-alias
```

### 2. 配置签名
在 `android/app/build.gradle` 中添加：
```gradle
android {
    signingConfigs {
        release {
            storeFile file('my-release-key.jks')
            storePassword '你的密码'
            keyAlias 'my-alias'
            keyPassword '你的密码'
        }
    }
    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
```

### 3. 构建 Release
```bash
gradlew.bat assembleRelease
```

### 4. 找到 APK
```
位置：android/app/build/outputs/apk/release/app-release.apk
```

---

## 📱 安装 APK 到手机

### 方法1：微信/QQ 传输
1. 将 APK 发送给自己
2. 手机上下载
3. 点击安装

### 方法2：数据线传输
1. 手机连接电脑
2. 复制 APK 到手机
3. 用文件管理器打开
4. 点击安装

### 方法3：二维码分享
1. 上传 APK 到网盘
2. 生成分享链接
3. 手机扫码下载

### 方法4：ADB 安装
```bash
adb install release/应用整理工具.apk
```

---

## ✨ 推荐流程（最简单）

```
1. 用 Android Studio 打开 android 文件夹
   ↓
2. 等待 Gradle 同步完成（看底部状态栏）
   ↓
3. 双击运行 build_apk.bat
   ↓
4. 等待 2-5 分钟
   ↓
5. 在 release 文件夹找到 APK
   ↓
6. 微信发给自己，手机下载安装
   ↓
7. 完成！
```

---

## 🎊 成功标志

当你看到以下情况，说明成功了：

✅ 在 `release` 文件夹看到 `应用整理工具.apk`  
✅ 文件大小约 5-10 MB  
✅ 手机上能正常安装  
✅ 打开后能看到手机上的所有应用  

---

## 💡 小提示

1. **首次构建会比较慢**
   - 需要下载很多依赖
   - 喝杯咖啡等待吧 ☕

2. **后续构建会很快**
   - 依赖已经缓存
   - 只需要编译代码

3. **Debug 版本够用了**
   - 自己用不需要 Release
   - Release 主要是上架应用商店用的

4. **APK 可以直接分享**
   - 发给朋友也能用
   - 但手机品牌不同可能需要调整权限

---

**现在就试试双击 `build_apk.bat` 吧！** 🚀
